name: CI - Build and Test

on:
  workflow_dispatch:
  pull_request:
    paths:
      - "lib/**"
      - "test/**"
    branches: [main]

concurrency:
  group: ci-$
  cancel-in-progress: true

jobs:
  build_tests:
    uses: demian-ilnytskyi/workflows/.github/workflows/package_ci_build_and_test.yml@main
    secrets: inherit
    with:
      flutter_versions: "3.16.0, 2.0.0"
